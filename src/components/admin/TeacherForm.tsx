import { TeacherFormView, type TeacherFormViewProps } from "./teacher-form/TeacherFormView";

export default function TeacherForm(props: TeacherFormViewProps) {
  return <TeacherFormView {...props} />;
}
